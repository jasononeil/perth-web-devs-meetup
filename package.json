{
  "name": "perth-web-devs",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply perth-web-devs-db --local",
    "db:seed": "wrangler d1 execute perth-web-devs-db --local --file=./migrations/0002_seed.sql"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "marked": "^15.0.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241004.0",
    "typescript": "^5.6.0",
    "wrangler": "^3.80.0"
  }
}
