{
    "$schema": "https://raw.githubusercontent.com/jetify-com/devbox/0.12.0/.schema/devbox.schema.json",
    "packages": ["bun@latest"],
    "shell": {
        "init_hook": [
            "test -e .envrc.private || cp .envrc.example .envrc.private"
        ],
        "scripts": {
            "test": ["echo \"Error: no test specified\" && exit 1"]
        }
    }
}
